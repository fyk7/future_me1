<div class="microposts-wrap">
  <div class="text-center" style="padding-bottom: 20px;">
    <h2>ハッシュタグ一覧</h2>
  </div>
<h5>
<% @tag.each do |tag|%>
  <h2><%= link_to(tag_back_to_hash(tag.name), "/microposts/hashtag/#{tag.name}", style: "font-size: #{tag_size(tag.microposts.count) + 4 }px;") %></h2>
<% end %>
</h5>
</div>